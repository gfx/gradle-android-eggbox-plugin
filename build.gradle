buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.1.0-rc2'
        classpath 'com.jakewharton.sdkmanager:gradle-plugin:+'
    }
}

allprojects {
    repositories {
        jcenter()
        mavenLocal()
    }
}

task('uploadArchives') {
}
